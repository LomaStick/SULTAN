export type IProduct = {
	barcode: number,
	title: string,
	imgurl: string,
	sizevalue: string,
	sizetype: string,
	manufacturer: string, 
	brand: string, 
	description: string, 
	price: number,
	currency: string,
	typeCategory: string,
	cartCount: 0;
}